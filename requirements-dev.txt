# Development dependencies for a local checkout. Pinned so a failure means the
# code changed, not a dependency. Runtime deps live in manifest.json.
#
# Lint and type checking run locally, not in CI: the ruff and mypy settings
# live in pyproject.toml, which is not part of this repository, so a CI runner
# would silently check against tool defaults instead. Home Assistant is here
# because the mypy settings are tuned against its type stubs.
homeassistant==2026.9.0
xmltodict==1.0.4
ruff==0.16.6
mypy==2.3.1

# The test suite is not part of this repository either; these are here so a
# fresh clone can set up the same local environment.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-homeassistant-custom-component==0.13.363
hypothesis==6.167.1
aioresponses==0.7.9
